<resources>
    <style name="Theme.Finn" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Keep icons and button styles if referenced in legacy XML; Compose pages ignore these. -->
        <item name="imageButtonStyle">@style/Widget.Finn.ImageButton</item>
        <item name="materialButtonStyle">@style/Widget.Finn.Button</item>
    </style>
    
    <style name="Theme.Finn.Launcher" parent="Theme.Finn" />
</resources>
